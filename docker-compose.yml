version: "3.1"

services:
  db:
    container_name: stage-container
    image: mysql
    ports:
      - 3306:3306
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=stage 
      - MYSQL_PASSWORD=stage